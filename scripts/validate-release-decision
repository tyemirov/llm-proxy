#!/usr/bin/env bash

set -euo pipefail

script_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
repository_root="$(cd "${script_root}/.." && pwd -P)"
cd "${repository_root}"

exec python3 scripts/validate_release_policy.py
