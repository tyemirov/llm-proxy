---
all:
  children:
    gateway:
      hosts:
        gateway-host:
          ansible_host: gateway.example.invalid
          ansible_user: deploy
          ansible_become: true
          ansible_become_method: sudo
          ansible_become_user: root
          mprlab_runtime_root: /srv/mprlab-runtime
          mprlab_docker_cli: /usr/local/bin/docker
          mprlab_compose_project_name: mprlab-nginx-gateway
